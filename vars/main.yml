---
bootstrap_environment:
  LANG: C
  LC_CTYPE: C
  LC_ALL: C
  LC_MESSAGES: C
  PKG_HOME: "{{coreos_pkg_home}}"
  PYPY_VERSION: "{{coreos_pypy_version}}"
  PYPY_FLAVOR: "{{coreos_pypy_flavor}}"
  PYPY_DOWNLOAD_URL: "http://cepcsoft.ihep.ac.cn/package/cepcenv/pypy/6.0/pypy-6.0.0-linux_x86_64-portable.tar.bz2"
  PYPY_SHA256: "{{coreos_pypy_sha256}}"
  COREOS_SSL_CERTS: "{{coreos_ssl_certs}}"
  ANSIBLE_FACTS_DIR: "{{ansible_facts_dir}}"
